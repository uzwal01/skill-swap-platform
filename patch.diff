*** Begin Patch
*** Update File: frontend/src/components/UserCard.tsx
@@
-type Props = {
+type Props = {
   user: User;
   onRequest?: (user: User) => void;
+  variant?: 'default' | 'large';
 };
 
-const UserCard: React.FC<Props> = ({ user, onRequest }) => {
+const UserCard: React.FC<Props> = ({ user, onRequest, variant = 'default' }) => {
@@
-  return (
-    <div className="rounded-lg border bg-white p-4 shadow-sm">
+  const pad = variant === 'large' ? 'p-6' : 'p-4';
+  const nameCls = variant === 'large' ? 'text-base font-semibold' : 'font-medium';
+  return (
+    <div className={ounded-lg border bg-white  shadow-sm}>
       <div className="mb-3 flex items-center gap-3">
         <div className="flex h-10 w-10 items-center justify-center rounded-full bg-gray-200 text-sm font-semibold">
           {initials}
         </div>
         <div>
-          <div className="font-medium">{user.name}</div>
+          <div className={nameCls}>{user.name}</div>
           <div className="text-xs text-gray-500">{user.email}</div>
         </div>
       </div>
*** End Patch
